/**
 * RSI-Alpha — useStreamAnalysis hook (stub)
 *
 * Placeholder hook. Implementation follows after Checkpoint 1 approval.
 */

export function useStreamAnalysis(_activityId: string) {
  return {
    data: null,
    isLoading: true,
    error: null,
    refetch: () => {},
  };
}
